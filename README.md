# 3D Model Generator from Image or Text

## Overview
Prototype for generating a basic 3D model using:
- A single image of an object
- A short descriptive text prompt

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
